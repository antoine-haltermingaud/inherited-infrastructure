# Context — 05-FixCommitAdversarialReview

- Target under review: commit `6f1771d` (the graded Part-2 fix), parent `a782e19`.
- Claims to judge against: that commit's message + `REVIEW.md` § "Top 3 findings to fix now".
- Delta context that decides severity: live production, millions of users, several
  deploys/day, no ops team, zero appetite for downtime.
- Evidence method: `git worktree add <tmp> a782e19`, `cdk synth` both sides, diff
  `cdk.out/*.template.json`. Template evidence outranks reading the TypeScript.
- Prior sessions' reasoning: `PARA/Tasks/02-TopFindingsFix/00.rejected-ideas.md`,
  `PARA/Tasks/03-AiNotes/01-disagreement-log.md` (the VPC-CIDR vs service-SG deviation
  is already logged there — do not re-litigate it as a new finding).
- Build gate: `npx cdk synth` + `npx tsc` must pass with no AWS credentials.
