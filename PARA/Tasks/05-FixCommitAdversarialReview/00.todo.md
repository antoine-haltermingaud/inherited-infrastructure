# Todo — 05-FixCommitAdversarialReview

- [x] Create task folder + GH issue (#6), status `doing`
- [x] Synth both sides (`2421410` worktree vs `655147d`), enumerate every resource delta
- [x] V1 unintended replacement — **FAIL**: ECS service gains `DeploymentController` (absent→`{Type:ECS}`),
      a replacement-triggering property. RDS + SG clean (no replacement triggers changed).
- [x] V2 rotation claim — PASS, `MasterUserPassword` resolves from the new secret, in-place
- [x] V3 secrets injection — PASS, execution role has `GetSecretValue`+`DescribeSecret` on that secret
- [x] V4 SG rule swap — PASS on substance (1 rule in, 1 rule out, no residual `0.0.0.0/0`);
      commit's "atomic … throughout" wording is overstated (revoke/authorize order is not guaranteed)
- [x] V5 deploy settings — PASS, 4 tasks place fine, no deadlock at min 100/max 200,
      `/health` is DB-independent so the circuit breaker won't false-trip during rotation
- [x] V6 backup/deletion/snapshot present in template; backup-init outage claim is accurate
- [x] V7 claims audit — **FAIL** (corrected from an initial PASS during the same session):
      commit violates REVIEW.md's absolute "never rotate and rewire in the same deploy" without
      disclosing it; "GetSecretValue granted to the execution role only" is false (task role is
      still `*`/`*`); step 2's connection-survival claim is cancelled by step 1's backup-init blip.
      Documented, not coded around — see `01-adversarial-review.md`.
- [x] (added) Re-verified V1 against a clean detached checkout of `655147d` after a second review
      pass wrongly "refuted" it by synthesizing the already-fixed working tree
- [x] Write `01-adversarial-review.md` (per-vector PASS/FAIL + evidence)
- [x] Fix + commit — `2af650b`, sets the CDK feature flag AWS recommends; pushed
- [x] Close issue with session comment; set `status: done`
