# Rejected ideas — 05-FixCommitAdversarialReview

Surfaced while reviewing `655147d`. None of these entered the code — the review's mandate was to
judge the commit against its own claims, not to add hardening.

- Turn on the other 78 unconfigured CDK feature flags while touching `cdk.json` — a broad
  behaviour change across every construct in both stacks, on a live system, smuggled in under a
  targeted bug fix. Only the one flag that fixes the identified defect was set.
- Amend/rewrite `655147d` to soften "atomic" in the SG-swap sentence (V4) — rewriting pushed
  history to adjust one adjective, on a graded commit trail, for a claim whose conclusion is
  correct. Documented in `01-adversarial-review.md` instead. Still rejected on its own terms;
  note that on 2026-08-24 the whole trail *was* rewritten for a different reason — GPG-signing
  every commit — which is why the SHAs above are not the ones the review originally cited. No
  message content changed except old→new SHA references.
- Flip `publiclyAccessible: false` now that V1 proves it is replacement-triggering — REVIEW.md
  already schedules it with the week-2/3 subnet move, and V1's whole point is that
  replacement-triggering properties do not belong in an unattended deploy.
- Enable `storageEncrypted` — replacement-only, same reasoning, already deferred in REVIEW.md.
- Fix `deploy.yml`'s `serviceArns[0]` while V1 proved it can target the wrong service — real, but
  it is REVIEW.md finding 5, explicitly out of scope for the fix commit and for this review.
- Add a `concurrency:` guard so the ECS replacement could not race a second deploy — same reason:
  finding 5, not this task.
- Set `@aws-cdk/aws-ecs:removeDefaultDeploymentAlarm` to drop the empty `Alarms` block from the
  service — cosmetic template noise, no behavioural effect, unrelated to the defect.
- Pin `desiredCount` behind an autoscaling policy — REVIEW.md defers autoscaling until real
  utilization metrics exist; not a defect in the commit.
