# Context — 11-TlsIngress

- The problem: `lib/service-stack.ts:74` — internet-facing ALB serves price data over HTTP :80 only. Anything on-path can read or ALTER prices in transit.
- Ranking: `REVIEW.md` finding 6 — moderate severity, but table stakes for financial data.
- The blocker is a decision, not code: what domain does pricefeed live on, and where is DNS hosted? ACM certs are free once that's answered.
- Consumers: internal Delta apps call this ALB — their base URLs must move to https; inventory who calls it.
