# Context — 09-GithubOidc

- The problem: `.github/workflows/deploy.yml:17-22` — long-lived IAM access keys in GitHub secrets; stolen keys work until someone notices.
- Ranking: `REVIEW.md` finding 5 (Friday slot); OIDC gives short-lived per-run credentials.
- Depends on: nothing hard, but scheduled after 08 so the trust policy is written against the final workflow shape.
- AWS side: OIDC identity provider for token.actions.githubusercontent.com + a deploy role; trust policy pinned to this repo and the main branch. Role permissions: assume the CDK bootstrap roles (deploy/file-publishing/image-publishing/lookup) + ECR push.
