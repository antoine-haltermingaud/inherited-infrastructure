# Context — 06-TaskRoleScoping

- The problem: `lib/service-stack.ts:41-46` — task role is `actions: ['*'], resources: ['*']` on a container terminating internet traffic.
- Ranking + migration risk: `REVIEW.md` finding 4 (account takeover via one SSRF/dependency compromise).
- Why it was NOT in the fix commit: `655147d` message + REVIEW.md top-3 — a scoped policy must come from CloudTrail evidence, not guesswork.
- Known intent: code comment says "S3 exports, parameter store" — treat as a hint, not an inventory.
- Evidence sources: CloudTrail event history for the role ARN; IAM Access Advisor last-accessed data; IAM Access Analyzer policy generation.
