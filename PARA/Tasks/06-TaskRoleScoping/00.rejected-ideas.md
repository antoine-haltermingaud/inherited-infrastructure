# Rejected ideas — 06-TaskRoleScoping

- Ship a guessed policy from the code comment alone — the REVIEW.md migration risk: silently breaks undocumented usage; how prod S3 exports die on day three.
- Include role scoping in the take-home fix commit — no CloudTrail evidence available in the exercise; rejected in `6f1771d` scope (see disagreement log).
- Permission boundary instead of fixing the policy — adds indirection, still leaves the broad policy as documentation of intent.
