# Requirements — 06-TaskRoleScoping

**Planned for Delta week one (Wednesday). Deliberately NOT implemented in the take-home** — requires CloudTrail access this exercise doesn't have.

## Acceptance criteria

- Usage inventory produced from CloudTrail + Access Advisor covering ≥30 days, reviewed with the team for undocumented periodic jobs (exports may be monthly).
- The `*`/`*` statement replaced by a policy scoped to evidenced services/resources (expected shape: specific S3 bucket paths + SSM parameter path).
- Deployed as a rolling update; logs watched for `AccessDenied` for 24h with a documented instant-rollback path (re-attach broad statement).
- The old statement removed only after the observation window.

## Constraints

- One permission class at a time if evidence is ambiguous; never cut everything blind.
