# Rejected ideas — 01-InfraReview

- Rank strictly by security severity (CVSS-style order) — rejected: the assignment weighs context fit; the guaranteed outage-per-deploy outranks several classic security items here.
- Merge findings 1 and 3 (exposed DB / no backups) into one — rejected: different causes and different fixes; kept adjacent with the interlock stated instead.
- Per-finding dollar cost model — rejected: false precision inside a 90-min box; order-of-magnitude only where it changes the decision (Fargate task, Multi-AZ, NAT).
- Findings as a table — rejected: the 1–2 sentence prose rationale *is* the evaluated artifact; tables hide reasoning.
- Write the ECS-vs-K8s kicker into REVIEW.md in this session — rejected: owned by task 04-EcsVsK8s, which appends it after this task flips to done (coordination agreed 2026-08-24).
