# Context — 13-HousekeepingBatch

- Source: `REVIEW.md` Housekeeping tier — "real but small; none justify their own deploy".
- Items: log retention INFINITE (`lib/service-stack.ts:31`); no ECR lifecycle policy (:24-26); hardcoded physical names (`repositoryName`, `logGroupName`); region duplicated in `bin/pricefeed.ts:10` + `deploy.yml:22`.
- NOT here: Dockerfile items — they ride with the pipeline rework (task 08), per REVIEW.md.
- Caution item: physical-name removal REPLACES the resources (new log group, new ECR repo) — cheap for logs, but the ECR repo rename must coordinate with whatever tag scheme task 08 lands.
