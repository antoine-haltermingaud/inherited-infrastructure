# Requirements — 13-HousekeepingBatch

**Planned for Delta "whenever convenient", as ONE batched PR. Deliberately NOT implemented in the take-home** — outside the top-3 scope cap.

## Acceptance criteria

- Log retention 30–90 days (team picks; default 90 for a pricing service).
- ECR lifecycle policy: keep last N tagged images (N sized to the rollback horizon from task 08), expire untagged.
- Region defined once (CDK context or env), consumed by both the app entrypoint and the workflow.
- Physical names: log group rename done opportunistically (old group left to age out); ECR repo rename ONLY if task 08 makes it safe, else explicitly skipped with a line here.
- One PR, one review, one deploy — verified as no-interruption changes in the template diff (except the accepted log-group replacement).

## Constraints

- If any item turns out to need its own migration story, it leaves this batch and becomes a task.
