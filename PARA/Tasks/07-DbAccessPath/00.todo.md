# Todo — 07-DbAccessPath

- [ ] Deploy 1: define app SG in DataStack, add SG-to-SG ingress rule alongside the VPC-CIDR rule
- [ ] Deploy 2: pass app SG to ServiceStack, attach to the Fargate service (verify rolling, not replacement, in template diff)
- [ ] Deploy 3: remove the VPC-CIDR rule
- [ ] Create EC2 Instance Connect Endpoint + its SG; add to DB ingress
- [ ] Write the team runbook: Workbench via EICE tunnel
- [ ] Have a teammate validate access; delete any leftover personal access habits
