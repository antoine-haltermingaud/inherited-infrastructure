# Requirements — 07-DbAccessPath

**Planned for Delta week one (Wednesday). Deliberately NOT implemented in the take-home** — the safe sequencing needs multi-deploy orchestration against a live stack.

## Acceptance criteria

- DB security group ingress = the app's security group + the team-access endpoint's security group. No CIDR-wide rule remains.
- No app→DB connectivity gap at any point: the app SG is defined in DataStack (so it exists before any rule references it), passed to ServiceStack, adopted by the service, and only THEN is the VPC-CIDR rule removed — three deploys, each independently safe.
- Team has a documented MySQL Workbench path via EC2 Instance Connect Endpoint; tested by one engineer who isn't me.
- ECS service update verified as a rolling network-config change, not a service replacement (check the template diff before deploying).

## Constraints

- Zero downtime throughout — every step must be individually revertible.
