# Context — 07-DbAccessPath

- Current state after `655147d`: DB ingress is VPC-CIDR-wide — internet exposure removed, but anything in the VPC can reach 3306, and the team lost their MySQL Workbench access (previously via `0.0.0.0/0`).
- Target state: `REVIEW.md` finding 1 + Wednesday plan — ingress from the *service* security group only, plus a sanctioned team access path.
- Why VPC-CIDR was shipped instead of SG-to-SG: `PARA/Tasks/03-AiNotes/01-disagreement-log.md` — cross-stack rule ordering opens an app→DB outage window (`cdk deploy --all` updates DataStack first); an explicit-SG swap risks ECS service network reconfiguration. This task owns doing it properly.
- Access path options: EC2 Instance Connect Endpoint (no instance to run) or SSM port forwarding (needs an agent host). Fargate-only VPC → EICE is the natural fit.
