# Todo — 02-TopFindingsFix

- [x] Pick the 2–3 findings to fix (blocked on `01-InfraReview` ranking)
- [x] (added) REVIEW.md was missing at session start (01 not done); picked from the verified findings inventory and scaffolded a stopgap REVIEW.md
- [x] (added) 01's parallel session landed the full REVIEW.md mid-session (superseding the scaffold); realigned the commit to its "Top 3 findings to fix now" spec — adopted desiredCount 2 + circuit breaker and the backup/deletion/removal trio, dropped the publiclyAccessible flip (review defers it to the subnet-move window), kept VPC-CIDR ingress over service-SG with rationale in the disagreement log
- [x] Implement the fixes
- [x] Verify `npx cdk synth` and `npx tsc` pass (verified against synthesized templates, no AWS creds)
- [x] Write the commit message (change / out-of-scope / rollout risk + mitigation)
- [x] Commit
