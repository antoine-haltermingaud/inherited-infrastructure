# Rejected ideas — 02-TopFindingsFix

- ~~`desiredCount: 2` in the same commit~~ — initially rejected on cost; **superseded**: REVIEW.md finding 2 prescribes it (~$20/month, and a lone task means any crash/AZ hiccup is a full outage). Adopted.
- SG-to-SG ingress rule — REVIEW.md prescribes it, still not shippable in this one commit: the service SG is implicit (created in ServiceStack), so the rule lands in ServiceStack and `cdk deploy --all` revokes the world-open rule (DataStack) minutes before the allow exists → app→DB outage window; making the SG explicit in DataStack instead risks ECS service replacement (network-config change). VPC-CIDR rule inside DataStack removes the same internet exposure atomically; SG-to-SG lands with the week-2–3 network window.
- Store the existing committed password in Secrets Manager (no rotation) — in CDK that re-commits the plaintext into git via the Secret resource, securing nothing. (REVIEW.md's Tuesday store-then-rotate two-step is an out-of-band console/CLI sequence a single IaC commit cannot express.)
- Scrub the password from git history — pointless once rotated; REVIEW.md agrees ("rotation, not history-rewriting, is what makes the leak harmless").
- Scope the task role in this commit — needs a CloudTrail usage inventory first; REVIEW.md defers it for the same reason.
- ~~Enable backups in this commit~~ — initially deferred because 0→N takes a brief RDS outage; **superseded**: REVIEW.md puts `backupRetention: 7` in the fix commit with the deploy timed to a low-traffic window. Adopted, with the window directive in the code comment and commit message.
- Flip `publiclyAccessible: false` now — free in-place hardening, but REVIEW.md explicitly groups it with the week-2–3 subnet move and the SG change already removes the reachable path. Dropped to match the review's commit spec.
