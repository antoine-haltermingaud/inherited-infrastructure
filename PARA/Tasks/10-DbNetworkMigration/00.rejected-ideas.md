# Rejected ideas — 10-DbNetworkMigration

- Do it in week one — REVIEW.md: SG lockdown already removed the exposure; rushing the riskiest migration for residual risk is bad sequencing.
- Enable encryption in place — RDS cannot; snapshot-copy-restore is the only path.
- Two separate windows (subnet move, then encryption) — two production freezes where one suffices; combine.
- Blue/green via read replica promotion — worth pricing for zero-downtime, but replica of unencrypted → encrypted has its own restrictions; evaluate in the runbook step, default remains snapshot/restore.
