# Rejected ideas — 08-DeployPipelineRework

- Fix the ARN-guessing step (name the cluster/service explicitly) — treating the symptom; SHA-tagged task defs make the step redundant. Delete it.
- Deploy by image digest only — immutable but human-opaque; SHA tags are traceable to a commit. (Digest pinning can be added later, not instead.)
- Blue/green via CodeDeploy — REVIEW.md "reasonable" list: rolling + health check + circuit breaker is the right machinery for this team size.
- Keep `npm install` — non-reproducible installs in CI; `npm ci` + lockfile or the gate is theater.
