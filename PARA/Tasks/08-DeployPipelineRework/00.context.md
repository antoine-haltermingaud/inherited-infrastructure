# Context — 08-DeployPipelineRework

- The problem: `.github/workflows/deploy.yml` — `:latest`-only images (:27-28), no test/typecheck/synth gate, `npm install` not `npm ci` (:32), no concurrency guard, force-redeploy aims at `clusterArns[0]`/`serviceArns[0]` (:35-39).
- Ranking: `REVIEW.md` finding 5 — "deploys are untraceable, unguarded, and aimed by luck"; the ARN-guessing breaks the day a second cluster appears in the account.
- Key insight from the review: SHA-tagged images make the force-redeploy step *unnecessary* (the task-def change itself triggers the rolling deploy), so the ARN-guessing is deleted, not fixed.
- Dockerfile items ride along per REVIEW.md housekeeping: `app/Dockerfile` has no `npm ci` (breaks the day a real dependency is added), runs as root, no lockfile.
- Test gate content: `npm ci && tsc && cdk synth` + existing `app/test.js`.
