# Todo — 08-DeployPipelineRework

- [ ] Wire image tag: workflow passes `-c imageTag=$GITHUB_SHA`, ServiceStack reads it (default for local synth)
- [ ] Add gate job: npm ci, tsc, cdk synth, app/test.js
- [ ] Add concurrency group
- [ ] Delete the force-new-deployment step
- [ ] Dockerfile: lockfile + npm ci --omit=dev, non-root USER, HEALTHCHECK
- [ ] Add app/package-lock.json (doesn't exist yet — required for npm ci)
- [ ] Dry-run: template diff shows only task-def image change
- [ ] Document + test rollback (redeploy previous SHA)
