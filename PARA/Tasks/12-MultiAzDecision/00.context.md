# Context — 12-MultiAzDecision

- Current state: `multiAz: false` (`lib/data-stack.ts:52`). An instance/AZ failure = extended outage; data loss is already covered by backups (`6f1771d`).
- Ranking: `REVIEW.md` finding 7 — deliberately NOT a week-one reflex: it roughly doubles the DB bill for a low-likelihood event; the review demands it be a PRICED decision.
- This is a decision task: the deliverable is the decision + its record, and the enable only if "yes".
- Pricing inputs: current t4g.medium single-AZ cost vs. Multi-AZ; what an hour of pricefeed DB outage costs Delta (millions of users) — ask the business side, don't guess.
