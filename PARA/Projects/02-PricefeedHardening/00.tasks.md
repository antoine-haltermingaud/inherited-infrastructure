# Tasks — 02-PricefeedHardening

In delivery order (day labels from REVIEW.md's week-one plan).

- [ ] `PARA/Tasks/06-TaskRoleScoping/` — Wed: scope the `*`/`*` task role from CloudTrail evidence (finding 4).
- [ ] `PARA/Tasks/07-DbAccessPath/` — Wed: SG-to-SG DB ingress + team access via EC2 Instance Connect Endpoint (finding 1 residual).
- [ ] `PARA/Tasks/08-DeployPipelineRework/` — Thu: SHA-tagged images, CI gate, concurrency, kill the ARN-guessing step (finding 5).
- [ ] `PARA/Tasks/09-GithubOidc/` — Fri: OIDC deploy role, delete long-lived AWS keys (finding 5).
- [ ] `PARA/Tasks/10-DbNetworkMigration/` — wk 2–3: private subnets + `publiclyAccessible: false` + storage encryption, one window.
- [ ] `PARA/Tasks/11-TlsIngress/` — month: ACM + HTTPS listener + redirect, after the domain/DNS decision (finding 6).
- [ ] `PARA/Tasks/12-MultiAzDecision/` — month: Multi-AZ as a priced decision, backups now exist (finding 7).
- [ ] `PARA/Tasks/13-HousekeepingBatch/` — whenever: log retention, ECR lifecycle, physical names, region duplication — one PR.
